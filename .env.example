# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Path to SQLite database (default: ./data/axiomgarden.db)
DATABASE_URL=./data/axiomgarden.db

# Full URL of the app (used by Auth.js)
NEXTAUTH_URL=http://localhost:3000
