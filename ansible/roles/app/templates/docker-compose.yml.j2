services:
  app:
    image: ghcr.io/fishybytes/axiomgarden:{{ app_version | default('latest') }}
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - /opt/axiomgarden/data:/app/data
    env_file:
      - .env
